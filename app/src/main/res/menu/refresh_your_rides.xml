<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/actionsearch"
        android:icon="@drawable/refresh_white"
        android:title="Refresh"
        app:showAsAction="ifRoom" />
</menu>


<!-- AIzaSyDWbQJxOw1BHAqQqUWL2DekIsYar5vUzxc

LayoutInflater inflater = (LayoutInflater) activity.getSystemService(Context.LAYOUT_INFLATER_SERVICE);
        View rowView = inflater.inflate(R.layout.activity_chat_singlemessage, parent, false);
        TextView textView = rowView.findViewById(R.id.singleMessage);
        textView.setText(String.valueOf(chatMessages.get(position)));
        // change the icon for Windows and iPhone
        String s = String.valueOf(chatMessages.get(position));
        if (s.startsWith("iPhone")) {
            imageView.setImageResource(R.drawable.no);
        } else {
            imageView.setImageResource(R.drawable.ok);
        }

        return rowView; -->